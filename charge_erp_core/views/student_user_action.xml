<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data noupdate="1">

        <!-- Server Action: Create User from Student -->
        <record id="action_create_student_user" model="ir.actions.server">
            <field name="name">Create Users</field>
            <field name="model_id" ref="model_op_student"/>
            <field name="binding_model_id" ref="model_op_student"/>
            <field name="state">code</field>
            <field name="code">
                records.action_create_user()
            </field>
        </record>

        <!-- Optional: Automated Action or Menu Integration -->
        <!--
        <record id="ir_cron_create_student_user" model="ir.cron">
            <field name="name">Auto Create Student Users</field>
            <field name="model_id" ref="model_op_student"/>
            <field name="state">code</field>
            <field name="code">
                records.action_create_user()
            </field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="active">False</field>
        </record>
        -->

    </data>
</odoo>

