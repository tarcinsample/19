<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="view_op_attendee_wizard_form" model="ir.ui.view">
            <field name="name">op.attendee.wizard.form</field>
            <field name="model">op.attendee.wizard</field>
            <field name="arch" type="xml">
                <form string="Add Attendees">
                    <group>
                        <field name="session_id"/>
                        <field name="attendee_ids"/>
                    </group>
                    <footer>
                        <button name="subscribe" type="object"
                                string="Subscribe" class="btn-primary"/>
                        <button string="Cancel" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="action_op_attendee_wizard" model="ir.actions.act_window">
            <field name="name">Add Attendees</field>
            <field name="res_model">op.attendee.wizard</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>

    </data>
</odoo>